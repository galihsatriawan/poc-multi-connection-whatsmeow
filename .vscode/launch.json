{
    "configurations": [        
        {
            "name": "safe",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}",
        }
    ]
}